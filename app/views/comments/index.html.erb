<h2>comments <%= link_to "(+)", new_comment_path %></h2>

<ul>
  <% @comments.each do |comment| %>
    <li>
      <%= link_to "#{comment.body} - #{comment.post.content}", comment %>
    </li>
  <% end %>
</ul>
