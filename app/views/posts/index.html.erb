<%= link_to "Create New Post", new_post_path %>
<section>
  <% @posts.each do |post| %>
    <article>
      <h1> <%= link_to post.content, post_path(post) %> </h1>
      <p><%= post.content %> </p>
    </article>
  <% end %>
</section>
